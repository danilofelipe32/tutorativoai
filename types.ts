
export enum ActionType {
    SUMMARIZE = 'SUMMARIZE',
    KEYWORDS = 'KEYWORDS',
    REFLECT = 'REFLECT',
    TEST = 'TEST',
    SIMPLIFY = 'SIMPLIFY',
    MINDMAP = 'MINDMAP',
    ANALOGY = 'ANALOGY',
    STEP_BY_STEP = 'STEP_BY_STEP',
    CONNECTIONS = 'CONNECTIONS',
}

export enum View {
    MAIN = 'MAIN',
    RESULTS = 'RESULTS',
}